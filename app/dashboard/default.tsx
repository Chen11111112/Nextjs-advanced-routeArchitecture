export default function DashboardDefault() {
  return (
    <div className="space-y-3">
      <h1 className="text-xl font-semibold">儀表板概覽</h1>
      <p className="text-sm leading-6 text-slate-400">
        這是 `children` 槽的 `default.tsx`，在硬重新整理後若無法還原主畫面狀態時會顯示。
      </p>
    </div>
  );
}
